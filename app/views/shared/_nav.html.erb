<nav>
  <div class="nav">
    <%= link_to 'TestGuru', root_url %>
  </div>

  <% if user_signed_in? %>

    <div class="nav user">
      <p>Welcome, <b><%= "#{current_user.first_name} #{current_user.last_name}" %></b> Guru |
        <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
      </p>
    </div>
   <% end %>
</nav>
